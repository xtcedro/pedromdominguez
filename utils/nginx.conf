# Redirect all HTTP to HTTPS (default)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    return 301 https://$host$request_uri;
}

# ---------------------------------------------
# www.domingueztechsolutions.com
# ---------------------------------------------
server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name www.domingueztechsolutions.com domingueztechsolutions.com;

    ssl_certificate /etc/letsencrypt/live/domingueztechsolutions.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/domingueztechsolutions.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_log /var/log/nginx/domtech_error.log;
    access_log /var/log/nginx/domtech_access.log;
}

# ---------------------------------------------
# www.heavenlyroofingok.com
# ---------------------------------------------
server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name www.heavenlyroofingok.com heavenlyroofingok.com;

    ssl_certificate /etc/letsencrypt/live/heavenlyroofingok.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/heavenlyroofingok.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_log /var/log/nginx/heavenlyroofing_error.log;
    access_log /var/log/nginx/heavenlyroofing_access.log;
}

# ---------------------------------------------
# www.okdevs.xyz
# ---------------------------------------------
server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name www.okdevs.xyz okdevs.xyz;

    ssl_certificate /etc/letsencrypt/live/okdevs.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/okdevs.xyz/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_log /var/log/nginx/okdevs_error.log;
    access_log /var/log/nginx/okdevs_access.log;
}

# ---------------------------------------------
# www.pedromdominguez.com
# Secure custom domain config + backend proxy
# ---------------------------------------------

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name www.pedromdominguez.com pedromdominguez.com;

    ssl_certificate /etc/letsencrypt/live/pedromdominguez.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pedromdominguez.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3003;  # ✅ Use your unique backend port
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_log /var/log/nginx/pedro_error.log;
    access_log /var/log/nginx/pedro_access.log;


}

# HTTP to HTTPS redirect for pedromdominguez.com
server {
    if ($host = www.pedromdominguez.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = pedromdominguez.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;

    server_name www.pedromdominguez.com pedromdominguez.com;

    return 301 https://$host$request_uri;




}

# ---------------------------------------------
# www.efficientmoversllc.com
# ---------------------------------------------
server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name www.efficientmoversllc.com efficientmoversllc.com;

    ssl_certificate /etc/letsencrypt/live/efficientmoversllc.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/efficientmoversllc.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_log /var/log/nginx/efficientmovers_error.log;
    access_log /var/log/nginx/efficientmovers_access.log;
}

# Optional: HTTP to HTTPS redirect for efficientmoversllc.com
server {
    if ($host = www.efficientmoversllc.com) {
        return 301 https://$host$request_uri;
    }

    if ($host = efficientmoversllc.com) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    listen [::]:80;

    server_name www.efficientmoversllc.com efficientmoversllc.com;

    return 301 https://$host$request_uri;
}